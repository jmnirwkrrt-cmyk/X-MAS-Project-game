if (!instance_exists(ask_parent)) {
	if (color = 0 and range > 0) draw_circle_colour(x, y, range, c_lime, c_lime, true);
	if (color = 1 and range > 0) draw_circle_colour(x, y, range, c_red, c_red, true);
}

